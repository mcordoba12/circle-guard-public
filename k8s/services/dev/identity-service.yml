---
# CircleGuard Identity Service - DEV Environment
# Port: 8083
# Type: ClusterIP (internal)

apiVersion: v1
kind: ConfigMap
metadata:
  name: circleguard-identity-service-config
  namespace: dev
data:
  SPRING_PROFILES_ACTIVE: "dev"
  SPRING_APPLICATION_NAME: "circleguard-identity-service"
  SERVER_PORT: "8083"
  LOGGING_LEVEL_COM_CIRCLEGUARD: "DEBUG"
  SPRING_FLYWAY_ENABLED: "false"
  SPRING_DATASOURCE_URL: "jdbc:postgresql://postgres.dev.svc.cluster.local:5432/circleguard_identity"
  SPRING_DATASOURCE_USERNAME: "admin"
  SPRING_KAFKA_BOOTSTRAP_SERVERS: "kafka-broker.dev.svc.cluster.local:9092"

---
apiVersion: v1
kind: Secret
metadata:
  name: circleguard-identity-service-secret
  namespace: dev
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: "password"
  JWT_SECRET: "dev-jwt-secret-key-circleguard-identity-service-12345"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: circleguard-identity-service
  namespace: dev
  labels:
    app: circleguard-identity-service
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: circleguard-identity-service
  template:
    metadata:
      labels:
        app: circleguard-identity-service
        environment: dev
    spec:
      serviceAccountName: circleguard-sa
      containers:
      - name: identity-service
        image: angela912/circleguard-identity-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8083
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: circleguard-identity-service-config
        - configMapRef:
            name: circleguard-infra-config
        env:
        - name: SPRING_FLYWAY_ENABLED
          value: "false"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres.dev.svc.cluster.local:5432/circleguard"
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: circleguard-identity-service-secret
              key: SPRING_DATASOURCE_USERNAME
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: circleguard-identity-service-secret
              key: SPRING_DATASOURCE_PASSWORD
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: circleguard-identity-service-secret
              key: JWT_SECRET
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-secret
              key: REDIS_PASSWORD
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          tcpSocket:
            port: 8083
          initialDelaySeconds: 180
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 5
        readinessProbe:
          tcpSocket:
            port: 8083
          initialDelaySeconds: 90
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 5
      imagePullSecrets:
      - name: acr-credentials
      restartPolicy: Always
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: circleguard-identity-service
  namespace: dev
  labels:
    app: circleguard-identity-service
    environment: dev
spec:
  type: ClusterIP
  selector:
    app: circleguard-identity-service
  ports:
  - port: 8083
    targetPort: 8083
    protocol: TCP
    name: http
